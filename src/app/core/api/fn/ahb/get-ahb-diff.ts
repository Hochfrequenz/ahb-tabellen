/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AhbDiff } from '../../models/ahb-diff';
import { FormatVersion } from '../../models/format-version';

export interface GetAhbDiff$Params {

/**
 * Pruefidentifikator to compare
 */
  pruefi: string;

/**
 * The newer format version to compare (FVxxxx code, 'current', or an ISO date YYYY-MM-DD)
 */
  'format-version-new': FormatVersion;

/**
 * The older format version to compare (FVxxxx code, 'current', or an ISO date YYYY-MM-DD)
 */
  'format-version-old': FormatVersion;
}

export function getAhbDiff(http: HttpClient, rootUrl: string, params: GetAhbDiff$Params, context?: HttpContext): Observable<StrictHttpResponse<AhbDiff>> {
  const rb = new RequestBuilder(rootUrl, getAhbDiff.PATH, 'get');
  if (params) {
    rb.path('pruefi', params.pruefi, {});
    rb.query('format-version-new', params['format-version-new'], {});
    rb.query('format-version-old', params['format-version-old'], {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AhbDiff>;
    })
  );
}

getAhbDiff.PATH = '/api/ahb-diff/{pruefi}';
